/*
// funcao sem retorno.

function imprimirSoma(n1, n2) {
  console.log(n1 + n2);
}

imprimirSoma(2, 3); // 5
imprimirSoma(2); // NaN
imprimirSoma(2, 8, 9, 10); // 10
imprimirSoma(); // NaN

// funcao com retorno.
function soma(a, b = 1) {
  return a + b;
}

console.log(soma(2, 3)); // 5
console.log(soma(2)); // 3

let testeSemRetorno = imprimirSoma(2, 4);
console.log(testeSemRetorno); // undefined

let testeComRetorno = soma(2, 4);
console.log(testeComRetorno); // 6
*/

/*
// Armazenando funcao em uma variavel.

const imprimirSoma = function (a, b) {
  console.log(a + b);
};

imprimirSoma(2, 3); // 5

// Armazenando uma funcao arrow em uma variavel.
const soma = (a, b) => a + b;
console.log(soma(2, 6)); // 8

*/
